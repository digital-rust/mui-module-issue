/* eslint-disable @typescript-eslint/indent */
import Button from '@mui/material/Button/Button.js';
import * as React from 'react';

export default function MyButton(): JSX.Element {
    return (
      <Button variant="contained">thebutton</Button>
    );
}
